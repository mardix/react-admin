import store, {actions} from '../../store'
import React, {Component} from 'react';
import { connect } from 'react-redux';
import  * as BS  from 'react-bootstrap';


class Logout extends Component {
    render = () =>
        <div />
}

export default connect((store) => {})(Logout)

